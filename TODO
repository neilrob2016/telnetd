alternate login/shell program to execute for individual users in password file
add user to "who" list in MacOS
If bad config file on SIGHUP don't quit, carry on with current settings
Allow more than one network interface to be specified in config
